# PDF Page Merger Project
This project uses Python and PyPDF2 to combine selected pages from multiple PDFs.

## How to use:
1. Open the .ipynb file to see the code(python_project)
2. The input files are provided in the folde(maths syallbus and python syallbus).
3. The final result(final_combined_syallbus).
